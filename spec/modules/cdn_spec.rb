require 'spec_helper'

describe CDN do
  describe "::has_image?" do
    it "should return true if the image is on S3, false of not" do
      pending "This is tested elsewhere in the specs"
    end
  end

  describe "::count" do
    it "returns the number of objects on the cdn" do
      pending "Only used in specs"
    end
  end

  describe "::url_for_image" do
    it "returns the cdn_url for an image" do
      pending "Just a convenience method for an Image method tested elsewhere"
    end
  end
end
