require 'spec_helper'

describe DeleteListingsWorker do
  describe "#perform" do
    it "deletes a listing by id if the id is in the DTQ" do
      pending "Example"
    end

    it "looks up the listing by url if only the url is in the DTQ" do
      pending "Example"
    end
  end
end
