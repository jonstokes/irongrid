describe 'spec_helper'

describe CreateLinksWorker do
  it "Spawns an UpdateListingsWorker instance when it starts" do
    pending "Example"
    #It spawns an UpdateListingsWorker.perform(domain: domain) to start populating
    #the LinkSet for this domain in parallel with the crawl. This way, existing listings
    #will have database ids for them and be updated.
  end
end
