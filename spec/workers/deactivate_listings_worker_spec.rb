require 'spec_helper'

describe DeactivateListingsWorker do
  it "deactivates a listing by id if the id is in the DAQ" do
    pending "Example"
    # daq.pop == { id: 1234 }
  end

  it "looks up the listing by url if only the url is in the DAQ" do
    pending "Example"
    # daq.pop == { url: "http://foo.com/1" }
  end
end
